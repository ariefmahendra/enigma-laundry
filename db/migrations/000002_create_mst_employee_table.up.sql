create table mst_employee(
                             id serial primary key,
                             name varchar(255),
                             phone_number integer,
                             address varchar(255)
);