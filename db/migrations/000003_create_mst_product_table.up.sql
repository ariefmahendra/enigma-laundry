create table mst_product(
                            id serial primary key,
                            name varchar(255),
                            unit varchar(255),
                            price integer
);
